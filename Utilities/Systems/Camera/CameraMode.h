#pragma once

// ============================================================
// カメラモード定義
// ============================================================
enum class CameraMode {
	DEFAULT,
	CLEAR,
	TITLE,
	FOLLOW,
	DEBUG,
	BATTLE_START,
	CLEAR_CINEMATIC,
};